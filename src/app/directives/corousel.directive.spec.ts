import { CorouselDirective } from './corousel.directive';

describe('CorouselDirective', () => {
  it('should create an instance', () => {
    const directive = new CorouselDirective();
    expect(directive).toBeTruthy();
  });
});
